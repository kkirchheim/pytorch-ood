With Optuna
===========

These examples drive pytorch-ood detectors from `Optuna <https://optuna.org>`__, whose
samplers (TPE, CMA-ES, Bayesian, multi-objective) search more efficiently than an
exhaustive grid. To run them, install Optuna:

.. code-block:: shell

    pip install optuna
